# Landing 1 on a14 — what running it actually found

Date: 2026-08-11
Machine: ZENBOOK-A14, Windows 11 build 26200, ARM64, user `kln`
Repo state: `main` at `a37edde` (pre-fix), then `8f27b40` (post-fix)
Build: `cargo build --all-targets`, **debug profile**, native `aarch64-pc-windows-msvc`

**Session note.** SSH lands in **session 0** — no desktop, no keyboard. Everything
below is either desktop-independent or explicitly marked as needing session 1.
Section 5 lists what is still owed.

---

## 1. The headline: the manifest stopped both binaries from starting

`beckon.exe --version` produced nothing at all. `beckon installed` returned
**0 lines in 15 ms**, against a documented catalog-scan cost of 370–500 ms —
which is what made it obvious the process was not running rather than running
badly.

Run through `cmd /c` to capture the real stream:

```
The application has failed to start because its side-by-side configuration
is incorrect. Please see the application event log or use the command-line
sxstrace.exe tool for more detail.
```

Application event log, provider `SideBySide`, id 59:

```
Activation context generation failed for
"C:\Users\kln\dev\beckon\target\debug\beckon.exe".
Error in manifest or policy file "...beckon.exe" on line 41.
Invalid Xml syntax.
```

**Cause:** three XML comments in `beckon.exe.manifest` contained `--`, which
is illegal inside an XML comment. Windows' SxS parser rejects the entire
manifest, and with it the process.

**This repo already knew.** `examples/windows/serve/beckon-serve.xml` opens
with *"XML comments cannot contain a double hyphen"* and works around it by
spelling the log option out in prose. The final review wave then wrote
``--log``, `Documented, not measured --` and ``no `CP_ACP` -- every string``
into manifest comments.

### What did not catch it

| Gate | Why it passed |
|---|---|
| Three code reviews | One *did* run an XML parser — before the wave that introduced the hyphens |
| The CI byte-search gate (Task 7) | Passes by design: the strings it looks for really are present |
| `x86_64-pc-windows-gnu` cross-check | Cannot see the manifest at all; `build.rs` embeds it for `-msvc` only |
| The host test suite | 188 tests, none of which start a Windows binary |

Fixed in `8f27b40`, together with the gate that does catch this class: **run
the built binary.** One process start, console subsystem only —
`beckon-serve.exe` answers `--version` with a `MessageBox`, which hangs a
runner with no desktop (observed here: it timed out in session 0).

Verified after the fix: `beckon.exe --version` → `beckon 0.7.0`, exit 0;
`beckon installed` → **122 lines**; zero new `SideBySide` events.

---

## 2. RT_MANIFEST is genuinely embedded

Read back out of the built binaries with Windows' own `FindResource`
(`LOAD_LIBRARY_AS_DATAFILE`, id 1, type 24) — not a byte search, so this
closes the hole Task 7's CI gate admits to.

| | `beckon.exe` | `beckon-serve.exe` |
|---|---|---|
| RT_MANIFEST present | yes, 3495 bytes | yes, 3495 bytes |
| `Microsoft.Windows.Common-Controls` | yes | yes |
| `PerMonitorV2` | yes | yes |
| `asInvoker` | yes | yes |
| `dpiAware` before `dpiAwareness` | yes | yes |

**PE subsystem unchanged**, which the manifest could have disturbed and did
not: `beckon.exe` = 3 (console), `beckon-serve.exe` = 2 (GUI). Matches what
`CLAUDE.md` recorded on 2026-08-11.

---

## 3. Hot-path cost of the manifest: none, measured with a control

The final review flagged that `beckon.rc` compiles into *every* binary, so the
one-shot `beckon <id>` path now resolves a comctl32 v6 activation context at
process start — on a path with a 50 ms budget and a documented 443→57 ms
optimisation history — and that nobody had re-measured it.

A/B on the same source: strip the manifest line from `beckon.rc`, rebuild,
measure; restore, rebuild, measure. Medians of 7 runs each, debug profile.

| | no manifest | with manifest | delta |
|---|---|---|---|
| `--version` (process-start floor) | 22.9 ms | 22.8 ms | **0.0 ms** |
| `resolve Terminal` | 307.6 ms | 309.4 ms | **+1.8 ms** |

**The control ran first and both builds started cleanly** — without that, a
harness that measures a non-starting process reads as a suspiciously fast
result, which is exactly the trap section 1 fell into before the fix.

**Caveat, stated because it matters:** these are **debug** builds. They are
not comparable to the 57 ms release figure in `CLAUDE.md`; only the *delta* is
meaningful, and the delta is inside the noise.

---

## 4. Environment notes worth keeping

- `~/.ssh/config` on the dev Mac carries `Include ~/.colima/ssh_config`, and
  every `ssh`/`scp` died with *"Operation not permitted"*. **Re-diagnosed
  2026-08-11 and the first reading was wrong:** it is not TCC. `~/.colima` is
  a symlink to `/Volumes/ssd/colima`, so the Include dangles whenever that
  volume is unmounted, and the message merely looks like a permission denial.
  With the SSD mounted, plain `ssh a14` and `scp` work with no flags at all;
  `colima` itself does not need to be running. Check `ls -d /Volumes/ssd`
  before reaching for the workaround (strip the Include into a temp config
  and pass `-F`).
- Plain `ssh a14 'powershell -Command "..."'` has its quoting destroyed.
  **This one is unrelated to the SSD and is still required**, re-tested
  2026-08-11 with the volume mounted: `-Command` still arrives mangled.
  `-EncodedCommand` with base64 UTF-16LE is the fix, and on this Mac
  `iconv -f UTF-8 -t UTF-16LE file | base64 | tr -d '\n'` is more reliable
  than the python one-liner (python3.14 here produced empty output silently,
  and `pyexpat` is blocked by system policy, so local XML validation is not
  available either — part of why the malformed manifest survived to hardware).
- `Start-Process` does **not** inherit `Set-Location`. A background build
  started that way ran in the wrong directory and produced a 0-byte log.

---

## 5. Still owed — needs session 1

None of this can be read in session 0; anything measured there is a false
negative by construction.

1. `GetDpiForWindow` before and after the manifest, on a 150 % display.
2. `lfMessageFont.lfFaceName` / `lfHeight` — the type ramp's baseline.
3. Themed control heights under comctl32 v6 (ListView row and header, button,
   edit) — these are the numbers Landing 2's spacing tokens must be tuned to.
4. Screenshots at 100 % and 150 %, with a pre-manifest control run.
5. `release_modifiers()`'s `VK_NONAME` filler: truncate a chord burst
   deliberately, fire the release, ask whether Start opened — with and without
   the filler, against a bare Win-tap control already proven to open Start.
6. `bound_keys` fed from registration results: fail one combo on purpose from
   another process, press `Caps+<that key>`, confirm **nothing** is injected.
7. The `dpiAware`/`dpiAwareness` ordering claim — still documentation-derived.
8. The tray icon at 150 %: `SM_CXSMICON` used to return a flat 16 and now
   tracks system DPI.

Route: a scheduled task in session 1, registered with a **SID** rather than
`DOMAIN\user` (a non-domain-joined machine fails with *"No mapping between
account names and security IDs was done"*), and
`New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries` — the `schtasks`
default refuses to start on battery and leaves the task `Queued` forever on a
laptop. Both recorded in `examples/windows/serve/README.md`.

---

# Session-1 pass — 2026-08-11, same machine

Route: a scheduled task named `a14probe`, principal a **SID**
(`S-1-5-21-…-1001`), `-LogonType Interactive`,
`New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries`. Payload delivered as
`-EncodedCommand`, output through a `.bat` redirect to a file.

**Control run first, as always:** the harness reported `session=1`,
`desktop-visible=True`, virtual screen `1280x800`. Without that, everything
below would be indistinguishable from a session-0 false negative.

**Display was at 100 % scale** (`GetDpiForSystem = 96`). Nothing at 150 % is
measured below; changing the scale would have altered a live signed-in
session, so it was not done.

> **WRONG — corrected in §12.** The display was at **150 %** the whole time.
> `GetDpiForSystem` returns a flat 96 to a DPI-unaware process, and §6 below
> records this harness as `UNAWARE`. The `1280x800` on the line above is the
> 150 % virtualization of a 1920x1200 panel and was the evidence sitting in
> plain sight.

## 6. beckon really is per-monitor DPI aware

| process | `GetProcessDpiAwareness` |
|---|---|
| **`beckon-serve.exe`** | **`PER_MONITOR_AWARE`** |
| PowerShell (control) | `UNAWARE` |

This is the premise the whole of Task 8 rests on, and it now has a
measurement instead of an inference. The control matters: a probe that
reported `PER_MONITOR_AWARE` for everything would look identical.

## 7. `lfMessageFont` — the spec's claim, confirmed

```
lfMessageFont.lfFaceName = 'Segoe UI'
lfMessageFont.lfHeight   = -12
lfMessageFont.lfWeight   = 400
iCaptionHeight = 22   iScrollWidth = 17   iMenuHeight = 19
SM_CXSMICON = 16      SM_CXICON = 32
```

`SPI_GETNONCLIENTMETRICS` returns **plain Segoe UI at 9 pt**, not Segoe UI
Variable — exactly as §B.3 of the spec says, now measured rather than cited.

> **Five more numbers here are virtualized too — corrected in §12.**
> `iCaptionHeight`, `iScrollWidth`, `iMenuHeight` (`SPI_GETNONCLIENTMETRICS`)
> and `SM_CXSMICON`, `SM_CXICON` (`GetSystemMetrics`) were read by the same
> DPI-**unaware** PowerShell harness that produced the wrong
> `GetDpiForSystem` recorded in §6 — both APIs are DPI-awareness-scaled
> exactly the way `GetDpiForSystem` is, not just the one number §12
> originally flagged (`lfMessageFont.lfHeight`). At this display's real
> 144 DPI (x1.5) a per-monitor-aware reader would get `iCaptionHeight = 33`,
> `iScrollWidth` ≈ 26 (25.5), `iMenuHeight` ≈ 29 (28.5), `SM_CXSMICON = 24`,
> `SM_CXICON = 48` — arithmetic, not re-measured, same caveat as §12/§13's
> derived columns (comctl32/user32 rounding could land either side of the
> fractional two). `SM_CXSMICON` is exactly what §15's "tray icon at 150 %"
> item is still owed.
>
> **What survives:** `Segoe UI`, weight `400`, and **9 pt** as the 96-DPI
> baseline. A point size is device-independent by definition, and face name
> / weight are string/int facts about which font got chosen — DPI awareness
> touches none of the three. Only the pixel-valued reads (`lfHeight` and the
> five metrics above) were wrong; this section's "confirmed" still holds for
> the font identity, just not for any pixel number in the block.

## 8. Font families — the spec was WRONG, and instructively

§B.3 claims Segoe UI Variable exposes **nine** GDI family names,
{Small, Text, Display} × {—, Light, Semib}, with "Semib" truncated to fit
`lfFaceName`'s 32-wchar buffer.

Measured: **twelve**, {Small, Text, Display} × {—, Light, Semibold,
Semilight} — the Semilight axis was missed entirely. And **the truncation is
not uniform**:

```
Segoe UI Variable Text Semibold     <- full, 31 chars
Segoe UI Variable Display Semib     <- truncated
Segoe UI Variable Small Semibol     <- truncated
Segoe UI Variable Text Semiligh     <- truncated
```

So code keyed on the "Semib" spelling finds the Display variant and **misses
the Text one**. Confirmed by the round trip that matters:

```
requested='Segoe UI Variable Text Semib'   got='Arial'     <- silent substitution
requested='Segoe UI Variable Text Semibold' got=(exact)
requested='This Font Does Not Exist'       got='Arial'     <- control
```

The control proves the substitution detector works. This is precisely why the
spec requires a `SelectObject` → `GetTextFace` round trip: `CreateFontW`
succeeded for the wrong name and quietly handed back Arial.

Also confirmed present and exact: `Segoe UI Variable Text` / `Small` /
`Display`, `Segoe Fluent Icons`, `Segoe MDL2 Assets`.

## 9. `caps_probe` — four measurements, all PASS, with a live control

```
RESULT 1: PASS - the injected chord fired our own RegisterHotKey
RESULT 2: PASS - the burst does not open Start
  CONTROL (bare Win tap): Start opened -> foreground SearchHost.exe
RESULT 4: PASS - injected VK_CAPITAL flipped the toggle (0 -> 1),
                 so caps_tap = "capslock" is implementable
RESULT 6: SendInput of 8 strokes took 3941 us  (~3.9 ms)
```

RESULT 1 is the gate the entire Caps-alias design rests on. RESULT 2's control
fired — a bare Win tap really did open Start in the same run — which is the
only thing that separates "the burst is safe" from "the detector is broken".
3.9 ms is 1.3 % of the 300 ms `LowLevelHooksTimeout`.

## 10. The settings window opens and is drivable under the manifest

`settings_probe` found the tray window, opened settings (`760x560` at 96 DPI,
which is what `scale()` yields at 100 %), enumerated 21 child controls, and
drove an edit character by character. The manifest did not break the window.

> **Units wrong — corrected in §12.** The window is **1140x840** at 144 DPI;
> `760x560` is what an unaware caller's `GetWindowRect` reports for it. The
> window was sized correctly, the probe was not reading it correctly.

The controls listed are still the pre-redesign set (`Keyboard` group box,
three tap radios) — correct, because Landing 1 deliberately does not touch
the layout.

## 11. Still owed

- ~~**Anything at 150 %.** The panel is at 100 % and raising the scale would
  disturb a live session. Needs a person at the machine, or a second display.~~
  **Void — the panel was already at 150 %; see §12. Everything in §13 is a
  150 % measurement. What is owed is the 100 % case.**
- ~~**Per-control themed heights under v6.** `settings_probe` prints class names
  but not rects, and it closes the window faster than an external poll can
  catch it (20 s of 50 ms polling missed it). Landing 2 tuning input, not a
  Landing 1 blocker. Cheapest fix: teach `settings_probe` to print each
  control's rect.~~ **Done — §13. The fix was the one predicted here.**
- **The `VK_NONAME` filler in isolation.** `caps_probe` exercises the chord
  burst, not `release_modifiers()`, so the filler added in Task 1 is still
  untested with-and-without. Task 10 step 4 still owes this.
- **The `dpiAware` / `dpiAwareness` ordering claim**, still documentation-only.
- **The tray icon at 150 %.**

---

# Landing 2a, Task 2 — themed control heights under comctl32 v6

Same machine (a14, Windows 11 Home build 26200, ARM64), 2026-08-11 22:37
local. Branch `landing-2a-settings-window` at `83a6aef`, built natively with
`cargo build --all-targets`, run in **session 1** through the `a14probe`
scheduled task (SID principal, `-LogonType Interactive`,
`-AllowStartIfOnBatteries` — the machine was on battery, so that flag was
load-bearing, not ceremonial).

## 12. The display is at 150 %, not 100 % — §5 and §10 are wrong

This is the finding that reorders everything else, so it goes first.

| reading | value | who read it |
|---|---|---|
| `GetDpiForWindow(settings window)` | **144** | `settings_probe`, per-monitor-aware |
| `GetDpiForSystem()` | **144** | `settings_probe`, per-monitor-aware |
| panel native mode (EDID, `WmiMonitorListedSupportedSourceModes`) | **1920x1200** | WMI, DPI-independent |
| `Screen.AllScreens[0].Bounds` | **1280x800** | PowerShell, DPI-**unaware** |

1920 / 1280 = 1.5 exactly. The scale factor falls out of two numbers that
never pass through `settings_probe` at all, so it does not depend on
trusting the probe.

§5 above records *"Display was at 100 % scale (`GetDpiForSystem = 96`)"* and
*"virtual screen `1280x800`"* in the same breath. Those two lines are the
same measurement disagreeing with itself: `1280x800` **is** the 150 %
virtualization of the panel's `1920x1200`. The `96` came from the PowerShell
harness, which §6 of this very file records as `UNAWARE` — and
`GetDpiForSystem` returns a flat 96 to an unaware process by design. The
harness measured its own awareness, not the display.

**Consequence for §10**: the `760x560` window recorded there is the
virtualized reading of a **`1140x840`** window. `scale(760, 144) = 1140`, so
the window was sized correctly the whole time; the probe was reporting it
divided by 1.5. Nothing was broken, but the number in §10 is in units nobody
declared.

**Consequence for §7, not re-measured here**: `lfMessageFont.lfHeight = -12`,
`iCaptionHeight = 22`, `iScrollWidth = 17`, `iMenuHeight = 19`,
`SM_CXSMICON = 16` and `SM_CXICON = 32` were all read by the same unaware
process, and all six are DPI-awareness-scaled the same way `GetDpiForSystem`
is — see the correction marker in §7 for the full breakdown. -12 is 9 pt at
96 DPI; at this display's real 144 DPI the per-monitor answer from
`SystemParametersInfoForDpi` would be -18. The five metrics would read `33`,
≈26, ≈29, `24` and `48` respectively (x1.5, arithmetic). The face name
(`Segoe UI`) and weight (400) are unaffected — a point size, a face name, and
a weight are not pixel geometry. **Flagged, not measured** — nothing in
Landing 2a re-read any of these six, and `ui_font()` already calls the
ForDpi variant for the font, so that half is a documentation defect and not
necessarily a code one; the five system metrics have no such mitigating
evidence yet.

**The fix, so this cannot recur**: `settings_probe` now calls
`SetProcessDpiAwarenessContext(PER_MONITOR_AWARE_V2)` as the first thing in
`run()` and prints the resulting awareness. It has to, and not only for
tidiness: `GetWindowRect` answers an unaware caller in virtualized pixels
while `LVM_GETITEMRECT` and `BCM_GETIDEALSIZE` come back through
`ReadProcessMemory` and are never virtualized. The unaware run printed a
29 px row next to a 21 px header **in the same block** when the truth is
29 and 31. Two unit systems, one table, no label.

## 13. The numbers Landing 2a's tokens are defined against

All physical pixels, at `GetDpiForWindow = 144`. The 96-DPI column is
`physical / 1.5` and is the unit the token constants are written in.

| what | at 144 DPI | at 96 DPI | source | chosen by |
|---|---|---|---|---|
| ListView row height | **29** | 19.33 | `LVM_GETITEMRECT(0, LVIR_BOUNDS)` | **comctl32** |
| ListView header height | **31** | 20.67 | `SysHeader32` rect, and item 0's `top` | **comctl32** |
| BUTTON height, as laid out | **39** | 26 | `GetWindowRect` | `btn_h = s(26)` |
| BUTTON height, theme's ideal | **33** | 22 | `BCM_GETIDEALSIZE` | **theme** |
| EDIT height (`IDC_COMBO`) | **36** | 24 | `GetWindowRect` | `row = s(24)` |
| COMBOBOX closed height (`IDC_APP`) | **33** | 22 | `GetWindowRect` | **theme** (see below) |
| COMBOBOX selection-field item height | **27** | 18 | `CB_GETITEMHEIGHT(-1)` | **theme** |
| `GetDpiForWindow` | **144** | — | — | display |

Read the last column before using any of these: **five** rows are
theme/comctl32-chosen (comctl32 picks the ListView row and header height;
the theme picks the button's ideal size, the combobox's closed height, and
its item height) and **two** are beckon's own tokens measured back out
(`btn_h = s(26)`, `row = s(24)`) — quoting those two as evidence for the
tokens they came from would be circular.

**The COMBOBOX row is theme-chosen and that is provable from the run.**
`layout` places `IDC_APP` with the same `row` height as everything else in
that column — the two `Static`s and the `Edit` beside it all came back 36 —
and the combo came back **33**. A non-owner-draw combo box snaps its closed
height to an integral item height plus borders, so it overrode the token:
27 + 2*3 = 33. `row = s(24)` is 2 logical px taller than the box the theme
actually draws.

**The BUTTON row is worth an eyebrow.** `btn_h = s(26)` is 39 physical
against an ideal of 33 — four logical pixels of slack. `BCM_GETIDEALSIZE`'s
`cx` also came back well under the placed width (`Add` 42 vs 105 placed,
`Remove` 72 vs 120, `Apply` 55 vs 126), which is expected: ideal `cx` is
text-fit, and beckon places buttons on a grid.

**`LVM_GETCOUNTPERPAGE = 16`** does not reconcile against a 2 px border:
(529 − 31 header) / 29 = 17.2, i.e. 17 full rows fit vertically
(17 × 29 = 493 ≤ 498), and a 2 px client border cannot explain losing a
whole row. The doc's own numbers explain it instead: the three
`LIST_COLUMNS` total **561** physical px (see the consistency check below),
but the list control itself is only **482** wide, so the content overflows
horizontally and comctl32 shows a horizontal scroll bar. That bar eats
vertical space — 498 px minus a scroll bar (`SM_CYHSCROLL`, ≈26 physical px
at this DPI) leaves 472, and `floor(472 / 29) = 16`, exactly the reported
count.

**This is a finding in its own right, not just an arithmetic correction.**
Spec §A.3 predicted "the default size already overflows" for `LIST_COLUMNS`
against the listview's default width, and this run is the first live
confirmation of that at 150 % — the column total (561) exceeding the
control's own width (482) by 79 px is exactly the overflow the spec warned
about, not a hypothetical. Task 6's token table needs the columns narrowed
(or the list widened) to remove the horizontal scroll bar, not just the
row-height numbers above.

> **Fixed, and measured on the same machine — see §17.** On
> `landing-2a-settings-window` at `05db60b` the list is two columns, not
> three, they sum to **1166** against **1166** of available width, and
> `WS_HSCROLL` is clear with `LVM_GETCOUNTPERPAGE = 8`. The horizontal
> scroll bar described above no longer exists, so the vertical space it was
> eating is no longer lost either. Do not re-derive the 16-vs-17 arithmetic
> from this section against the current build; it describes the pre-landing
> layout.

Raw evidence, verbatim:

```
probe DPI awareness: PER_MONITOR_AWARE (v2 requested) (SetProcessDpiAwarenessContext: ok)
GetDpiForSystem: 144
PASS: settings window opened: 0x1a0680
  rect:    1140x840 at (228, 228)
  -- geometry (themed, comctl32 v6, inside the manifested process) --
    GetDpiForWindow:      144
    client area:          1118x784
    SysListView32 IDC_LIST (as built): 482x529 at (15,15)
      LVM_GETITEMCOUNT:     3
      LVM_GETCOUNTPERPAGE:  16
      LVM_GETITEMRECT(0):   561x29 at (0,31)  => ROW HEIGHT 29
      header SysHeader32:   482x31  => HEADER HEIGHT 31
    BUTTON   IDC_ADD: 105x39   BCM_GETIDEALSIZE 42x33
    BUTTON   IDC_REMOVE: 120x39   BCM_GETIDEALSIZE 72x33
    BUTTON   IDC_APPLY: 126x39   BCM_GETIDEALSIZE 55x33
    EDIT     IDC_COMBO:   591x36
    COMBOBOX IDC_APP:     591x33 closed   CB_GETITEMHEIGHT(-1) 27
```

`LVM_GETITEMRECT`'s width of 561 is its own consistency check: the three
`LIST_COLUMNS` are 34 + 190 + 150 = 374 at 96 DPI, and `scale(374, 144)` is
561. A virtualized read would have said 374.

## 14. The control run behind §12 and §13

Printed by the harness from inside the scheduled task, before anything was
measured. Every number above is void without it — an SSH shell lands in
session 0, where a probe that finds nothing looks exactly like a probe that
found a zero-height control.

```
CONTROL harness SessionId:      1
CONTROL explorer.exe in session: yes (pid 5756)
CONTROL top-level windows visible from this window station: 10
CONTROL   e.g. ApplicationFrameHost, brave, explorer, explorer, explorer, explorer
CONTROL screen \\.\DISPLAY1 bounds 1280x800 primary=True (virtualized: PowerShell is DPI-unaware)
CONTROL panel native mode: 1920x1200
  beckon-serve pid 6300 SessionId 1
tray window found: 0x1409dc
PASS: settings window opened: 0x1a0680
```

The probe reaching the tray window and opening the settings window is the
second half of the control: both live in `beckon-serve`, in session 1, so a
session-0 run cannot fake either. The listview was measured twice — once as
built (3 rows) and once after the probe's own Add (4 rows) — and reported
29 / 31 both times, so the row height is not an artefact of one row count.

## 15. Still owed after Task 2

- **Anything at 100 %.** The inversion of what §11 said: this display is at
  150 %, and the token constants are written in 96-DPI units, so the base
  case is now the untested one. The 96-DPI figures in §13 are arithmetic
  (`/1.5`), not measurements, and comctl32 rounds — a real 96-DPI run could
  land on 19 or 20 for the row, not 19.33.
- **`lfMessageFont` at the real DPI** — see §12; flagged, not measured.
- **The tray icon at 150 %** — still owed, and now known to be the *default*
  state of this machine rather than a special case. §7's correction gives
  `SM_CXSMICON = 24` by arithmetic (x1.5); nothing has read it from a
  per-monitor-aware process yet.
- **Not a Task 2 finding, but the run reproduced it**: `Apply stayed
  disabled` at the end of `drive_an_edit`, because typing `Notepad` into the
  App combo left `Debuggable Package Manager` in the field. That is the
  documented v6 auto-complete rewrite (`CBN_EDITCHANGE` carries pre-rewrite
  text), unchanged by this task.

---

# Landing 2a on a14 — the settings window, run on real hardware

Date: 2026-08-12
Repo state: `landing-2a-settings-window` at **`05db60b`**, working tree clean
Build: `cargo build --all-targets`, debug, native `aarch64-pc-windows-msvc`
Display: **150 %** (`GetDpiForWindow` = 144), unchanged — a live signed-in session
Power: **on battery**, 64 %, Balanced

Ten tasks rebuilt this window and none of the Win32 half had ever been run.
This is that run. Working notes, with every command and the verbatim probe
output, are in
`.superpowers/sdd/2026-08-11-settings-window-landing-2a/hardware-pass-report.md`.

## 16. The control run

Every number below is void without this, and the harness produced four
false negatives before it was trusted (all four are written up in the
working notes — a `\$S` heredoc bug, redirect metacharacters in a scheduled
task argument, below-normal task priority on battery, and an `EnumWindows`
call made from session 0).

**Session 1, with a desktop**, printed from inside the scheduled task:

```
Image Name                     PID Session Name        Session#    Mem Usage
========================= ======== ================ =========== ============
beckon-serve.exe             11760 Console                    1     16,068 K
```

**The probe reached the window** — the tray window and the settings window
both live in `beckon-serve`, in session 1, so a session-0 run cannot fake
either:

```
tray window found: 0x17025c
PASS: settings window opened: 0x380a28
  title:   "beckon - test20.toml"
  rect:    1290x960 at (38, 38)
  22 child controls:
```

**Cross-process `GetObjectW` is real** — the control for §18. A font created
in the probe's own process reads back exactly what went in:

```
CONTROL A same-process GetObjectW: rc=92 face="Comic Sans MS" h=-20 w=700
```

**The typing test carries its own control.** `settings_probe` types into the
plain `EDIT` (`IDC_COMBO`) as well as the `COMBOBOX` (`IDC_APP`). The EDIT
half passed 16/16 in the same run in which the COMBOBOX half failed 7/7, so
the probe's timing is sound and §19's combo result is not a race.

Two probes ran against a **20-row synthetic config** in a scratch directory.
The user's real `~/.config/beckon/apps.toml` was never opened by any of
this; its SHA-256 was compared against a backup afterwards and is
byte-identical, and the autostart Run value is unchanged.

## 17. The list — the defect the landing exists to kill is dead

| Measured (physical px @144) | Value | How |
|---|---|---|
| list window rect | 1220 x 292 | `GetWindowRect` |
| list client rect | 1192 x 290 | `GetClientRect` |
| header height | 34 | `SysHeader32` rect |
| row height | 32 | `LVM_GETITEMRECT(0, LVIR_BOUNDS)` |
| `LVM_GETCOUNTPERPAGE` | **8** | — |
| style | `0x50A1800D` | `WS_HSCROLL` **clear**, `WS_VSCROLL` set |
| `GetScrollInfo(SB_HORZ)` | min 0, max 1165, page 1166 | page > range ⇒ nothing to scroll |
| columns | 2, widths **866 + 300 = 1166** | `LVM_GETCOLUMNWIDTH` |
| `SM_CXVSCROLL` @144 | 26 | `GetSystemMetricsForDpi` |

**Item 3 — exactly 8 whole rows, no sliver of a ninth. PASS, and it is
exact arithmetic, not a judgement call:** 34 (header) + 8 × 32 (rows) = 290
= the client height, to the pixel. There is no room left for a ninth row and
no partial row to clip. The screenshot shows eight rows and a clean bottom
edge.

**Item 4 — no horizontal scroll bar. PASS.** `WS_HSCROLL` is clear and the
horizontal scroll page (1166) exceeds its range (1165), which is comctl32's
way of saying the content fits. Compare §13, where three columns totalling
561 px inside a 482 px list forced a bar that then ate a row of vertical
space. That is gone.

**Item 5 — the columns fit the space exactly. PASS.** 866 + 300 = **1166**,
and the client width less the vertical scroll bar is 1192 − 26 = **1166**.
`LVM_GETITEMRECT(0)` independently reports a row width of 1166, and the
horizontal scroll page is 1166. Four independent numbers agree.

## 18. The type ramp — asserted by face name, not by size

`WM_GETFONT` + `GetObjectW` → `lfFaceName`, per control. **This is the only
field that discriminates**: `make_font` writes the requested face into the
`HFONT` *only* when GDI really produced it, and otherwise builds the font
from the shell's `lfMessageFont`, whose face is plain `Segoe UI`. So a
fallen-back role is visible here and nowhere else — size and weight are
deliberately preserved across the fallback and prove nothing.

| Role | Face measured | `lfHeight` | `lfWeight` | Controls |
|---|---|---|---|---|
| Subtitle | **`Segoe UI Variable Text Semibold`** | −30 | 600 | `IDC_LBL_SECTION` |
| Body | **`Segoe UI Variable Text`** | −21 | 400 | list, header, both labels, combo, both edits, every button |
| Caption | **`Segoe UI Variable Small`** | −18 | 400 | `IDC_NOTES` |

**Item 2: PASS.** All 22 controls were read; **not one reported plain
`Segoe UI`**, so no role fell back. The heights are exact at this DPI:
20 / 14 / 12 logical px × 1.5 = 30 / 21 / 18 physical, which is what came
back.

## 19. What is broken

### Item 11 — the App combo still lies. **FAIL.**

The recorded failure was field `"Notepad"` / cell `"d"`. It is still there,
in the same shape, and the probe's re-read at +300 ms says it is not a
timing artefact:

```
typed 'N' -> field "Narrator"   list "N   not installed" <<< DISAGREES ... STILL WRONG, not slow
typed 'o' -> field "Obsidian"   list "o   not installed" <<< DISAGREES ... STILL WRONG, not slow
typed 't' -> field "Tailscale"  list "t   not installed" <<< DISAGREES ... STILL WRONG, not slow
typed 'e' -> field "Excel"      list "e   not installed" <<< DISAGREES ... STILL WRONG, not slow
typed 'p' -> field "Paint"      list "p   not installed" <<< DISAGREES ... STILL WRONG, not slow
typed 'a' -> field "Access"     list "a   not installed" <<< DISAGREES ... STILL WRONG, not slow
typed 'd' -> field "Debuggable Package Manager" list "d   not installed" <<< STILL WRONG, not slow
per-keystroke agreement: Shortcut field PASS (0 wrong, 0 slow), App field FAIL (7 wrong, 0 slow)
```

The screen says `Narrator`; the model recorded `N`. Every keystroke
disagrees. The plain `EDIT` beside it agreed on all 16 of its keystrokes in
the same run, which is the control.

**The dropdown half is fixed** — `05db60b`'s mouse-pick backstop works:

```
field: before "Debuggable Package Manager" -> after arrows "Developer Command Prompt for VS 2022" -> after Enter "..."
PASS: list "Developer Command Prompt for VS 2022" carries the picked value
```

So the defect is now specific: **picking from the dropdown is correct;
typing into the combo is not.**

### Item 13 — `defid` can be stranded. **CONFIRMED STRANDED.**

The hypothesis in the checklist was right. Measured, in order:

```
before:                banner hidden, reload hidden, defid=0x534B03EF  (id 1007 IDC_APPLY)
config touched on disk, banner appeared: True
banner shown:          reload visible=True enabled=True, defid=0x534B03EF (1007)
after focusing Reload: defid=0x534B03F7  id=1015   <- IDC_RELOAD
after Reload clicked:  banner visible=False reload visible=False
                       defid=0x534B03F7  id=1015   <- STILL IDC_RELOAD, now hidden
```

`show(reload, false)` does **not** raise `BN_KILLFOCUS`, so the default
button id is left pointing at a hidden control. This is reachable by
ordinary use — clicking Reload with the mouse focuses it, and clicking
Reload is the only way to use it — after which Enter addresses a control
that is not on screen instead of Save.

### Item 9 — the checkbox marks but does not select. **FAIL (with a caveat).**

Measured twice, the second time from a verified-clean model:

```
before: row 0 tickImg=1 selected=False selIndex=-1 Save=False shortcut="" app=""
after : row 0 tickImg=2 selected=False selIndex=-1 Save=False shortcut="" app=""
ITEM 9 mark            : PASS - row ticked
ITEM 9 select (LVIS)   : FAIL - LVIS_SELECTED did not move
ITEM 9 select (editor) : FAIL - editor fields unchanged
```

Two independent readings of "did the select callback fire" — comctl32's
`LVIS_SELECTED`, and whether the editor fields below followed the clicked
row — both say no.

**The caveat, stated because it changes what the fix is:** not selecting on
a state-image click is comctl32's *native* behaviour, and the click was
injected with `PostMessage` rather than made by a hand. So this is a
confirmed description of what the control does, but whether it is a beckon
defect or the toolkit's default that the spec wanted overridden is a design
question this pass cannot settle. Clicking a row's **text** does select it
and does populate the editor (measured in the item 7 run below).

## 20. The read-only path — it works, by exactly one route

**Item 12: PASS**, but only once the right route was found, and the two
wrong ones are worth recording because the obvious reading of the checklist
picks one of them.

**Route A — `beckon-serve <broken.toml>`: never gets there.**
`cmd_serve_app` parses the file *before* it installs the tray, so a startup
parse failure returns `Err` and `beckon-serve` shows a modal error dialog.
Measured: the process stayed alive with **no tray window**, and a session-1
`EnumWindows` found the dialog waiting for a human that was not there:

```
hwnd=1968592 pid=14000 proc=beckon-serve class="#32770" vis=True title="beckon serve"
```

(A first look for that dialog was made from session 0 and reported "none" —
a false negative by construction, corrected here.)

**Route B — break the file while the window is already open: no reaction.**
The watcher fires and the hotkey reload fails loudly in the log —

```
beckon serve: reload failed, keeping current shortcuts: ...: TOML parse error at line 3, column 22
```

— but the open window keeps its old contents and its notes never change.
`settings_unreadable` is only ever set by `load_settings_model`, which runs
when the window **opens** (and on Reload); nothing recomputes it for an open
window. The "file changed on disk" banner is what the open window offers
instead, and that banner does work (§19, item 13).

**Route C — break the file, then open the window: this is the supported
path, and it is good.**

```
ITEM 12 opens        : PASS - window opened on an unreadable file
  read-only  List=DISABLED Add=DISABLED Remove=DISABLED Save=DISABLED Caps=DISABLED
             Shortcut=DISABLED App=DISABLED || OpenFile=enabled Close=enabled
  ITEM 12 explains   : 5 note line(s)
     "!!  beckon cannot read this file, so it is open read only and nothing here can be changed."
     "!!  The problem is on line 3: "ctrl+super+alt+e" ="
     "!!  What went wrong: expected `"`, `'`"
     "!   Press Open config file to fix it in a text editor. This window turns editable on its own as soon as the file reads."
     "!   beckon never writes over a file it cannot read."
  ITEM 12 plain words: PASS - it says the file cannot be read
  ITEM 12 editing off: PASS - Add and Save are disabled
  ITEM 12 escapes    : PASS - Open config file and Close both enabled
```

Every editing control is disabled, both escape routes stay live, and the
explanation names the line and the reason in plain words.

**Item 10 — the notes strip does not clip. PASS.** The read-only state is
the richest one the window has, at **five** notes rather than the four the
checklist named:

```
notes control 1220x193 face="Segoe UI Variable Small" h=-18
widest line 930px: "!   Press Open config file to fix it in a text editor. ..."
ITEM 10 horiz: have 1220 need  930 => FITS
ITEM 10 vert : have  193 need  120 (5 lines x 24) => FITS
```

The `Warn` glyph's trailing space is doing its job — the screenshot shows
the text after `!` and `!!` on a common left edge.

## 21. The rest of the checklist

**Item 1 — controls found. PASS.** Class `BeckonSettingsWindow`, 22 child
controls, ids 1001–1020 all present and identified.

One observation, not a failure: the `ComboBox`'s own internal `EDIT` child
reports control id **1001**, which collides with `IDC_LIST`. It is harmless
today — `GetDlgItem` searches only immediate children, so it still resolves
1001 to the list, and the combo repackages its child's notifications under
its own id — but it is the same *shape* as the `-1` collision this window
already tripped over once, and it is invisible unless something walks the
tree recursively.

**Item 6 — after Add the new row is visible AND selected. PASS.**

```
count 21 -> 22; newRow=21 top=14 perPage=8 => visibleWindow=[14..21]
ITEM 6 visible : PASS
ITEM 6 selected: PASS  (LVM_GETNEXTITEM(LVNI_SELECTED)=21, Selected(newRow)=True)
```

The list scrolled to bring the new row into view rather than staying put.

**Item 7 — ticks and the selection survive typing. PASS.**

```
after text click: row 1 tickImg=1 selected=True selIndex=1 shortcut="ctrl+super+alt+b" app="Calculator"
before typing:    row 1 tick=True selected=True selIndex=1
after typing 'z': row 1 tick=True selected=True selIndex=1 shortcut="zctrl+super+alt+b"
ITEM 7 tick      : PASS - tick survived
ITEM 7 selection : PASS - selection survived (index unchanged)
```

An earlier run of this check reported a selection failure; **that reading
was wrong and is corrected here** — it had inspected a row that had been
ticked via the item 9 path and was therefore never selected in the first
place. Re-run against a genuinely selected row, both halves pass.

**Item 8 — a tick alone does not enable Save. PASS.** From a model verified
clean at open (`Save enabled at open = False`), ticking a row left Save
disabled. An earlier attempt was inconclusive because its own `Add` had
already dirtied the model; the clean-model re-run is the one that counts.

## 22. Still owed — needs a person at the screen

Unchanged in kind from §15: a probe cannot settle these. Two screenshots
were captured so they can be settled without another remote session —
`settings-valid.png` (20-row config) and `settings-readonly.png` — but the
judgements themselves are open.

- **Row density.** Now measurable but not yet judged: **32 physical px at
  144 DPI = 21.3 logical**, against comctl32's natural ~19 and the spec's
  tabulated 32. Note the asymmetry the run exposes — buttons are 48 physical
  (32 logical × 1.5, i.e. DPI-scaled) while the list row is 32 physical,
  which is the tabulated number used *unscaled*. Whether that is intended is
  a design call; the numbers are now on the record.
- **`EDIT` vs `COMBOBOX` vertical alignment.** Measured: the `EDIT`
  (`IDC_COMBO`) is 300x43 at y=399, the `COMBOBOX` (`IDC_APP`) is 744x36 at
  y=403. Their centres are 420.5 and 421 — within half a pixel — but the
  EDIT is 7 px taller, so the two boxes are concentric and unequal. Whether
  that reads as misaligned is for a human.
- **Whether the Subtitle loses descenders at any scale.** Not tested at any
  scale but 150 %.
- **`mark_glyph`'s `OK` prefix leading the note text.** No state reached in
  this pass produced an `Ok` note, so the `OK` prefix was never rendered.
  The `!!` and `!` prefixes were, and they align.
- **The two long Caps captions at Body 14 px.** Both render in full in the
  screenshot with no clipping, but the aesthetic judgement is open.
- **Anything at 100 %.** Still the untested base case, as in §15.
- **Whether Enter after Reload actually fires the hidden control** (§19,
  item 13). The stranded `defid` is measured; the consequence of pressing
  Enter in that state is inferred from it, not observed.

## 23. Checklist result

| # | Item | Result |
|---|---|---|
| 1 | class + control ids 1001–1007 | PASS |
| 2 | type ramp asserted by **face name** | PASS — no role fell back |
| 3 | exactly 8 whole rows, no ninth sliver | PASS — 34 + 8x32 = 290 exactly |
| 4 | no horizontal scroll bar | PASS — `WS_HSCROLL` clear |
| 5 | columns = client width − vscroll | PASS — 1166 = 1166 |
| 6 | after Add, new row visible **and selected** | PASS |
| 7 | ticks + selection survive typing | PASS |
| 8 | a tick alone does not enable Save | PASS |
| 9 | checkbox click fires mark **and** select | **FAIL** — mark only |
| 10 | notes strip renders without clipping | PASS — 5 notes, fits |
| 11 | App combo agrees with the list cell | **FAIL** — 7/7 keystrokes wrong |
| 12 | broken file opens read-only | PASS — via open-after-break only |
| 13 | `defid` stranded on a hidden Reload | **CONFIRMED STRANDED** |
